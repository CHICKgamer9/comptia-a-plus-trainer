declare const data: {
  generatedAt: string;
  seedCounts: Record<string, number>;
  totals: Record<string, number>;
  minPaths: number;
  domains: import("../types").Domain[];
};
export default data;
