import { pack } from "../../pack.mjs";

export const CODING_MORE = pack("coding", [
  ["vy-c-pr-small", "Craft", "A pull request is a conversation, not a dump of the term",
    "Small diffs, a why in the description, a screenshot if it is UI. Reviewers cannot hold 40 files of ‘misc’ in a head. Your future self is a reviewer too.",
    "One PR at the end called final_real. Review is a rubber stamp.",
    "Branch, do one job, open the PR when tests you have can run. Reply to comments with code or a reason, not a shrug.",
    "A force-push after someone reviewed rewrites the conversation. Ask before you rewrite shared history."],
  ["vy-c-gitignore", "Craft", "A .gitignore is a civic duty to the repo",
    "Secrets, __pycache__, node_modules, a .env, an OS junk file. Committing a 200 MB folder is how clones die. Ignoring source by accident is the other fail.",
    "Git should remember everything, including your password file, so you never lose it.",
    "Ignore build artefacts and secrets. Track the lockfile your team actually uses. Check git status before the first commit.",
    "A secret that was committed is not ungitted by deleting it later. Rotate the key. History still has it."],
  ["vy-c-readme-run", "Craft", "A README is how a stranger runs the thing on Thursday",
    "What it is, how to install, one command that works, a screenshot. ‘It works on my machine’ is not a document. Markers are strangers.",
    "The code is self-explanatory if you named a file app.py. READMEs are for people who cannot read code.",
    "Write the happy-path commands from a clean folder. Then a known limitation in one line.",
    "A README that only lists technologies you googled is a resume, not a runbook."],
  ["vy-c-dead-code", "Craft", "Commented-out code is a graveyard that still trips people",
    "Version control already remembers. A 80-line block of ‘just in case’ is how nobody knows which function is live. Delete, commit, recover if you must.",
    "Comment it out forever so you can paste it back in a panic. Git is optional.",
    "If you need it later, a commit hash is the bookmark. Leave a why if you must keep a disabled path behind a flag.",
    "A TODO without an owner is a decoration. Either do it or file it."],
  ["vy-c-magic-number", "Craft", "A magic number is a policy hiding in a literal",
    "86400, 0.1, 42. Name it (SECONDS_PER_DAY, GST_RATE). The next editor should not have to reverse-engineer a tax from a decimal.",
    "Literals are faster to type, therefore more professional. Names are for beginners.",
    "Promote a repeated literal to a named constant near the top. Then change it once.",
    "π to two places in a geometry program is a magic and a rounding error. Use a library constant."],
  ["vy-c-error-message", "Craft", "An error message is a user interface",
    "‘Error’ is not a message. Say what failed, what was expected, what to try. Log the gory bit; show the human bit. Students mark this without noticing they mark it.",
    "Stack traces in the UI look technical, therefore trustworthy. Print everything.",
    "Catch the recoverable, name the file/line in logs, and tell the user a next step that is not ‘ask the developer’.",
    "A silent except: pass is how you ship a mystery. That is the opposite of a message."],
  ["vy-c-test-first-tiny", "Craft", "A tiny test is a specification you can run",
    "One function, a few asserts, an edge (empty, 0, a weird string). Tests that only hit the happy path are a brochure. You do not need a framework to start.",
    "If it printed once, it is tested. Print is a test runner.",
    "Write the assert before you get clever. When it fails, you wanted that fail. Then fix.",
    "A test that hits the network for a school assignment is a flake machine. Fake the response if you can."],
  ["vy-c-consistent-style", "Craft", "A formatter is a peace treaty",
    "Tabs vs spaces, quotes, line length. Pick a tool (Black, Prettier) and stop arguing in review. Consistency is a kindness; clever alignment is a hobby.",
    "Style is self-expression. Mix tabs and spaces to show range.",
    "Run the formatter before you push. Match the repo, not your home laptop’s mood.",
    "A linter warning you ignore 400 times is not a warning; it is wallpaper. Fix or configure, do not train blindness."],
  ["vy-c-secrets-env", "Safety", "A secret belongs in the environment, not in the source",
    "API keys, a database password, a school login. .env is a local file you do not commit. A key in a screenshot on a slide is still a key.",
    "Hide it in a comment. Comments are invisible to git and to attackers.",
    "Use env vars or a secrets manager. Rotate anything that ever sat in a repo or a chat.",
    "A public GitHub search for AKIA and password= is a hobby for other people. Do not be the result."],
  ["vy-c-path-join-root", "Safety", "User-supplied filenames need a jail, not a string glue",
    "If a user can pick a filename, they may try parent-directory walks. Join with a library, then check the resolved path still sits inside the folder you meant.",
    "Filenames from users are just names. The operating system will not allow anything naughty because it is an operating system.",
    "Canonicalise the path, reject anything outside the root, and never concatenate raw strings into a shell command.",
    "A download endpoint that takes a path is a classic exam and a classic incident. Treat it as hostile."],
  ["vy-c-sql-param", "Safety", "String-gluing SQL is how you invent a drop table",
    "Parameter placeholders exist so the database sees data as data. A name with a quote is a person, not a command — unless you glue.",
    "Escaping quotes yourself is fine if you are clever. Also: trust school project users.",
    "Use the driver’s parameters. Then least-privilege DB accounts so a SELECT user cannot DROP.",
    "OR 1=1 is a cartoon. Real injections are quieter. Still do not glue."],
  ["vy-c-xss-escape", "Safety", "Untrusted text in HTML is a script unless you escape",
    "A comment box that echoes <script> is a classic. Templates that auto-escape are a seatbelt. innerHTML from a user is a choice.",
    "If it is their own comment they cannot hack themselves. XSS is a theoretical exam word.",
    "Escape on output, or use textContent. Markdown is still HTML at the end — sanitise if you render it.",
    "A stored XSS waits in the database for the next admin. That is why escaping is not optional on ‘just a school blog’."],
  ["vy-c-csrf-state", "Safety", "A logged-in browser will happily POST if a foreign page asks",
    "CSRF is the other site using the victim’s cookies. SameSite cookies and a token on state-changing forms are the usual seatbelts. GET should not buy a fridge.",
    "If they are logged in they meant it, even if the button was on another origin. Browsers know intent.",
    "Use framework CSRF protection on POST/PUT/DELETE. Do not put money-moves on a GET link.",
    "A ‘delete everything’ <a href> in an email is a CSRF cousin plus a human. Methods matter."],
  ["vy-c-dep-pin", "Safety", "An unpinned dependency is a stranger in next week’s install",
    "Left-pad was a parable. Lockfiles exist. npm install latest on a due-date morning is a plot. Audit is a habit, not a vibe.",
    "Always take the newest everything. New is patched. Version numbers are cowardice.",
    "Commit the lockfile, review major bumps, and do not copy a random GitHub gist’s install line into prod.",
    "A typosquat package name is a phishing of the fingers. Check the publisher."],
  ["vy-c-log-pii", "Safety", "Logs are a second database of people",
    "Passwords, tokens, a full credit card, a student’s mark. Debug print in production is how a pastebin incident starts. Retention is a policy.",
    "Log everything so you can debug. Privacy is a later coat of paint.",
    "Redact secrets, minimise fields, and do not ship school data to a public log drain.",
    "A screenshot of a log in a help ticket can be a disclosure. Crop."],
  ["vy-c-rate-limit", "Safety", "A public endpoint without a rate limit is a homework for bots",
    "Login, search, a SMS send. Brute force is a loop. Even a school project can melt a free API quota and a bill.",
    "If it is only a school assignment nobody will find the URL. Security by unlisted is a plan.",
    "Throttle logins, lock after N, and do not return ‘that email exists’ if you can avoid an oracle.",
    "A 429 is a kindness. Retry-After is a protocol. Busy-loops that ignore it are a rude client."],
  ["vy-c-semantic-html", "Web", "A button is a button; a div with a click is a trap",
    "Real <button> and <a href> come with keyboard, focus, and semantics. Clickable divs fail tab users and some crawlers. Role soup is a last resort.",
    "Everything can be a div. HTML5 is a rumour. CSS can fake a button perfectly, therefore it is a button.",
    "Use the element that matches the job. Links navigate; buttons do an action. Test with a keyboard only.",
    "A <a> without href that only has onclick is a button in a lie. Screen readers will tell on you."],
  ["vy-c-form-label", "Web", "A label is for the ear and the mouse, not a visual caption only",
    "for= and id= (or wrap the control). Placeholder is not a label — it vanishes. Required fields need a word, not only a red star that is an image.",
    "If the box is obvious you do not need a label. Obvious is a sighted developer at 100% zoom.",
    "Associate labels, name the error next to the field, and do not rely on colour alone.",
    "Autocomplete attributes are a gift to tired humans and a hint to browsers. Use them for name/email."],
  ["vy-c-alt-text-job", "Web", "Alt text describes the job of the image, or it is empty on purpose",
    "A chart needs the claim; a decorative swirl wants empty alt. filename IMG_4032 is not alt. Linked images need a link purpose.",
    "Alt should be a short joke or ‘image’. Length is optional. Also: stuff every keyword for SEO as a paragraph.",
    "Write what a person who cannot see the pixels still needs. If it is noise, empty alt.",
    "A CAPTCHA that is only an image is a lock on the door for some users. Provide an alternative."],
  ["vy-c-http-cache-header", "Web", "Cache-Control is a promise about staleness, not a speed spell",
    "A HTML page you just fixed can still look old in a phone. Cache-bust a hashed filename for assets; be careful caching personal HTML. 304 is a conversation.",
    "Hard refresh once on your machine means the world updated. Caches are local to you.",
    "Version static files, set short cache on HTML if it changes, and remember CDNs have their own memory.",
    "A ‘disable cache’ tick in devtools is not a production header. Users do not have your tick."],
  ["vy-c-cors-not-auth", "Web", "CORS is a browser rule, not a lock on the data",
    "A server without CORS will still answer curl. CORS stops a stranger’s webpage from reading the response in a user’s browser. It is not authentication.",
    "If CORS is set to * the API is secure because modern. Stars are locks.",
    "Put real auth on the API (session, token). Then CORS as a browser courtesy for the fronts you intend.",
    "A mobile app is not a browser CORS client in the same way. Do not ‘fix’ a missing token with Access-Control-Allow-Origin as a personality."],
  ["vy-c-status-code-honest", "Web", "A status code is a contract; 200 with an error blob is a lie",
    "404 missing, 401 unauthenticated, 403 forbidden, 400 bad input, 500 the server tripped. Frontends branch on these. Lying 200s make every client a detective.",
    "Always 200 so the spinner stops. The JSON will explain. Codes are for pedants.",
    "Pick the code that matches the outcome. Then a stable error shape {error, code}.",
    "A 302 on a failed login can be a pattern; a 500 on ‘wrong password’ is a leak and a mess."],
  ["vy-c-css-specificity-fight", "Web", "Specificity is arithmetic; !important is a surrender",
    "Inline > id > class > element, roughly, then source order. A 0,1,0 vs 0,2,0 fight is why ‘it works in the demo’ dies in the page. Layers (cascade layers) are a later peace.",
    "Put !important on everything so you win. Winning is design.",
    "Lower the specificity of the old rule or raise the new one honestly. Inspect computed style, do not guess.",
    "A 400-line selector from a copied snippet is a time bomb. Simplify the selector rather than stacking."],
  ["vy-c-progressive-enhance", "Web", "HTML that works without the script is a gift; JS is a layer",
    "A form that can POST without a SPA bootstrap will still work when the CDN of frameworks has a bad day. Progressive enhancement is not anti-JS; it is a spine.",
    "If it needs a framework it is modern. Noscript users are hypothetical.",
    "Make the core action work in HTML first. Then enhance. Test with JS off once as a humility.",
    "A link that is only a click handler with no href is a dead end for a crawler and a middle-click."],
]);
